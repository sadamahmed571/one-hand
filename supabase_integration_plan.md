# خطة ربط الموقع بقاعدة بيانات Supabase لعرض البيانات لحظياً (Real-time)

تهدف هذه الخطة إلى نقل التطبيق من الاعتماد على `localStorage` إلى قاعدة بيانات سحابية متطورة توفر مزامنة فورية للبيانات بين جميع المستخدمين ولوحة التحكم.

## 1. الخطوات التقنية للربط

### أولاً: إعداد مشروع Supabase
1. إنشاء مشروع جديد على [Supabase](https://supabase.com/).
2. إنشاء الجداول التالية:
   - **settings**: لتخزين إعدادات الحملة (سعر السهم، الهدف، رابط الواتساب).
   - **donations**: لتخزين بيانات المتبرعين والأسهم (الاسم، الهاتف، الحالة: معلق/مكتمل).
   - **ticker**: لتخزين رسائل شريط الأخبار المتحرك.
   - **wallets**: لتخزين بيانات محافظ الدفع.
   - **invitations**: لرصد دعوات المشاركة التي يقوم بها المستخدمون.

### ثانياً: تهيئة مكتبة Supabase في المشروع
- إضافة رابط المكتبة في صفحات HTML:
  ```html
  <script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
  ```
- إنشاء ملف `supabase-config.js` يحتوي على مفاتيح الربط (URL & Anon Key).

### ثالثاً: تفعيل خاصية Real-time
- تفعيل خيار **Realtime** في جداول `donations` و `ticker` من لوحة تحكم Supabase.
- استخدام وظيفة الاستماع للتغييرات في `index.html`:
  ```javascript
  supabase
    .channel('public:donations')
    .on('postgres_changes', { event: 'INSERT', schema: 'public', table: 'donations' }, payload => {
      // إظهار التنبيه (Toast) فوراً عند إضافة تبرع جديد
      showLiveToast(payload.new.donor, payload.new.shares);
      // تحديث العدادات والرسوم البيانية تلقائياً
      refreshUI();
    })
    .subscribe();
  ```

---

## 2. أفكار إبداعية لتطوير المبادرة

### 1. خريطة التبرعات التفاعلية (Interactive Heatmap)
بدلاً من الخريطة الثابتة، يمكن عرض نقاط مضيئة تظهر لحظياً على خريطة اليمن في مكان المتبرع الجديد فور إتمامه لعملية المشاركة، مما يعطي شعوراً بالتفاعل المجتمعي الواسع.

### 2. لوحة شرف المتصدرين (Leaderboard)
إضافة قسم "أكثر المدن مساهمة" أو "كبار المبادرين" (مع خيار إخفاء الاسم)، لخلق نوع من التنافس المحمود في عمل الخير.

### 3. عداد الأسهم الحي (Animated Live Counter)
استخدام تأثيرات حركية (Counter Up) تتغير تلقائياً أمام أعين الزوار دون الحاجة لتحديث الصفحة، مع تغيير لون العداد إلى الذهبي المشع عند اقتراب الحملة من هدفها.

### 4. شهادات شكر "ذكية"
ربط نظام الشهادات بـ Supabase بحيث يمكن للمتبرع العودة لاحقاً وإدخال هاتفه لاستخراج شهادته مرة أخرى، أو مشاركة رابط الشهادة مباشرة كموقع ويب مصغر خاص به.

### 5. بوت واتساب (WhatsApp Bot Integration)
ربط قاعدة البيانات بـ Webhook يقوم بإرسال رسالة شكر آلية للمتبرع فور قيام الأدمن بتغيير حالة تبرعه من "معلق" إلى "مكتمل".

---

**ملاحظة:** الانتقال إلى Supabase سيضمن عدم ضياع البيانات في حال مسح المتصفح (Cache) وسيوفر أماناً أعلى بكثير للمعلومات الحساسة.
